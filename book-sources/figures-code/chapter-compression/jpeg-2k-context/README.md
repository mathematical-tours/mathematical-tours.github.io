# Figure 6.6

Compression.

## Figure 6.6

Removed from the book at the author’s request.

Last published caption (LaTeX):

```tex
Coding order and context for JPEG-2000 coding.\relax
```

Omitted from the current comparison PDF. Stable identifier: `compression--jpeg-2k-context`.

Matching asset directory: `figures/chapter-compression/jpeg-2k-context`. `context.tex` records the mathematical context at reconstruction time. `original/` preserves historical assets and `original.pdf` assembles the previous figure. `proposed.pdf` is used by the reading editions only when the manifest state is `accepted`.

# Figure 5.3

Linear and Nonlinear Approximation.

## Figure 5.3

Previously accepted TikZ drawing retained in the reading editions.

Exact current book caption (LaTeX):

```tex
Hard thresholding.\relax
```

Regenerated from the accepted editable TikZ figure; its geometry is retained. The chapter retains coefficients only when their magnitude is strictly greater than T. The diagonal branches therefore have open endpoints at +/-T, while the zero branch has closed endpoints there. Vertical jump segments are omitted because they are not part of the function graph.

Omitted from the current comparison PDF. Stable identifier: `approximation--thresholding-hard`.

Rebuild from the repository root:

```sh
build/figure-runtime/bin/python scripts/regenerate_figures.py --id approximation--thresholding-hard
```

Matching asset directory: `figures/chapter-linear-and-nonlinear-approximation/thresholding-hard`. `context.tex` records the mathematical context at reconstruction time. `original/` preserves historical assets and `original.pdf` assembles the previous figure. `proposed.pdf` is used by the reading editions only when the manifest state is `accepted`.

The TikZ sources and `proposed.tex` specify the editable drawing and its assembly.

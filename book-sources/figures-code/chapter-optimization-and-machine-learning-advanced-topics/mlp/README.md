# Figure 15.12

Optimization \& Machine Learning: Advanced Topics.

## Figure 15.12

Original retained at the author’s request; the rejected proposal is archived.

Exact current book caption (LaTeX):

```tex
Computational graph for a feedforward architecture. \relax
```

Omitted from the current comparison PDF. Stable identifier: `optim-ml-advanced--mlp`.

Matching asset directory: `figures/chapter-optimization-and-machine-learning-advanced-topics/mlp`. `context.tex` records the mathematical context at reconstruction time. `original/` preserves historical assets and `original.pdf` assembles the previous figure. `proposed.pdf` is used by the reading editions only when the manifest state is `accepted`.

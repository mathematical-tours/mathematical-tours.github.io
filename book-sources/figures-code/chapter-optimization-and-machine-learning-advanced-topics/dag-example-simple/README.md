# Figure 15.10

Optimization \& Machine Learning: Advanced Topics.

## Figure 15.10

Original retained at the author’s request; the rejected proposal is archived.

Exact current book caption (LaTeX):

```tex
Example of a simple computational graph. \relax
```

Omitted from the current comparison PDF. Stable identifier: `optim-ml-advanced--dag-example-simple`.

Matching asset directory: `figures/chapter-optimization-and-machine-learning-advanced-topics/dag-example-simple`. `context.tex` records the mathematical context at reconstruction time. `original/` preserves historical assets and `original.pdf` assembles the previous figure. `proposed.pdf` is used by the reading editions only when the manifest state is `accepted`.

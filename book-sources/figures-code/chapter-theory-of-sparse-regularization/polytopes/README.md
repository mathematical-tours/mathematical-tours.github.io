# Figure 11.3

Theory of Sparse Regularization.

## Figure 11.3

Original retained at the author’s request; the rejected proposal is archived.

Exact current book caption (LaTeX):

```tex
The linear map $A$ projects the $\ell ^1$ ball $B$; the nonlinear recovery map $\Aa $ selects solutions of~\eqref {eq-lasso-constr}. \relax
```

Omitted from the current comparison PDF. Stable identifier: `sparse-theory--polytopes`.

Matching asset directory: `figures/chapter-theory-of-sparse-regularization/polytopes`. `context.tex` records the mathematical context at reconstruction time. `original/` preserves historical assets and `original.pdf` assembles the previous figure. `proposed.pdf` is used by the reading editions only when the manifest state is `accepted`.

# Figure 14.11

Optimization \& Machine Learning: Smooth Optimization.

## Figure 14.11

Previously accepted TikZ drawing retained in the reading editions.

Exact current book caption (LaTeX):

```tex
Contraction constant $h(\tau )$ for a quadratic function.\relax
```

Regenerated from the accepted editable TikZ figure; its geometry is retained. The highlighted curve is the maximum of the two absolute-value branches, with μ and L explicitly identified as the smallest and largest eigenvalues of C. Its minimum is at 2/(L+μ), and h(τ)<1 exactly for 0<τ<2/L. The drawing states its representative ratio L/μ=3; other labels remain symbolic.

Omitted from the current comparison PDF. Stable identifier: `optim-ml-smooth--grad-desc-contract`.

Rebuild from the repository root:

```sh
build/figure-runtime/bin/python tools/regenerate_figures.py --id optim-ml-smooth--grad-desc-contract
```

Matching asset directory: `figures/chapter-optimization-and-machine-learning-smooth-optimization/grad-desc-contract`. `context.tex` records the mathematical context at reconstruction time. `original/` preserves historical assets and `original.pdf` assembles the previous figure. `proposed.pdf` is used by the reading editions only when the manifest state is `accepted`.

The TikZ sources and `proposed.tex` specify the editable drawing and its assembly.

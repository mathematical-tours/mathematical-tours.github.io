# Figure 14.9; Figure 14.9 (continued)

Optimization \& Machine Learning: Smooth Optimization.

## Figure 14.9

Previously accepted TikZ drawing retained in the reading editions.

Exact current book caption (LaTeX):

```tex
Panels 1 and 2: first-order Taylor approximations in one and two dimensions. Panels 3 and 4: the gradient is orthogonal to the level set; the diagram illustrates the proof.\relax
```

Regenerated from the accepted editable TikZ figure; its geometry is retained. The line is the exact tangent to the representative quadratic at x. Both f(z) and T_x(z) are labeled at their respective marked points, making the vertical Taylor remainder explicit. The tangent value at the selected z is 2.5466. The tangent plane is computed for the displayed representative paraboloid and touches it at the marked point. The graph and its supporting plane are distinct objects.

Omitted from the current comparison PDF. Stable identifier: `optim-ml-smooth--expansion-taylor`.

Rebuild from the repository root:

```sh
build/figure-runtime/bin/python scripts/regenerate_figures.py --id optim-ml-smooth--expansion-taylor
```

## Figure 14.9 (continued)

Previously accepted TikZ drawing retained in the reading editions.

Exact current book caption (LaTeX):

```tex
Panels 1 and 2: first-order Taylor approximations in one and two dimensions. Panels 3 and 4: the gradient is orthogonal to the level set; the diagram illustrates the proof.\relax
```

Regenerated from the accepted editable TikZ figure; its geometry is retained. At the leftmost point of an elliptical level curve, the tangent is vertical and the outward gradient is horizontal. The right-angle marker records orthogonality, not a descent step. The arrows are normals to elliptical level curves, not radial arrows except on the principal axes. Level values increase outward. A separate key identifies the three complete ellipses, and the drawing explicitly states that arrow lengths are schematic: their directions are the mathematical content.

Omitted from the current comparison PDF. Stable identifier: `optim-ml-smooth--expansion-taylor-continued`.

Rebuild from the repository root:

```sh
build/figure-runtime/bin/python scripts/regenerate_figures.py --id optim-ml-smooth--expansion-taylor-continued
```

Matching asset directory: `figures/chapter-optimization-and-machine-learning-smooth-optimization/expansion-taylor`. `context.tex` records the mathematical context at reconstruction time. `original/` preserves historical assets and `original.pdf` assembles the previous figure. `proposed.pdf` is used by the reading editions only when the manifest state is `accepted`.

The TikZ sources and `proposed.tex` specify the editable drawing and its assembly.

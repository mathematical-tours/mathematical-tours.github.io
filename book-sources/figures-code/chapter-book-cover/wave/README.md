# Book cover

Book Cover.

## Book cover

Original retained at the author’s request; the rejected proposal is archived.

Exact current book caption (LaTeX):

```tex
Book cover: wave
```

Omitted from the current comparison PDF. Stable identifier: `cover--wave`.

Matching asset directory: `figures/chapter-book-cover/wave`. `context.tex` records the mathematical context at reconstruction time. `original/` preserves historical assets and `original.pdf` assembles the previous figure. `proposed.pdf` is used by the reading editions only when the manifest state is `accepted`.

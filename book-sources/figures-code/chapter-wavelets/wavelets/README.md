# Figure 4.28

Wavelets.

## Figure 4.28

Accepted reconstruction, integrated into the book and independent chapters.

Previous audit identifier: **4.27**.

Exact current book caption (LaTeX):

```tex
Daubechies mother wavelets $\psi $ with increasing numbers $p$ of vanishing moments.\relax
```

The mother wavelets are generated by the Daubechies cascade for p=1,2,3,4. Support length grows as 2p-1. Alternating moments of the scaling filter, which characterize the zero at the Nyquist frequency, are checked through order p-1. Overall wavelet sign follows the library’s documented filter convention.

Omitted from the current comparison PDF. Stable identifier: `wavelets--wavelets`.

Rebuild from the repository root:

```sh
build/figure-runtime/bin/python scripts/regenerate_figures.py --id wavelets--wavelets
```

Matching asset directory: `figures/chapter-wavelets/wavelets`. `context.tex` records the mathematical context at reconstruction time. `original/` preserves historical assets and `original.pdf` assembles the previous figure. `proposed.pdf` is used by the reading editions only when the manifest state is `accepted`.

`generate.py` specifies the numerical experiment; `provenance.json` records inputs, parameters, random seeds and checks.

# Figure 2.21

Fourier and Convolution.

## Figure 2.21

Previously accepted TikZ drawing retained in the reading editions.

Exact current book caption (LaTeX):

```tex
Spherical coordinates.\relax
```

Regenerated from the accepted editable TikZ figure; its geometry is retained. Theta is colatitude from the positive north axis, matching the chapter formula. Phi is equatorial azimuth. The point, its equatorial projection, both angle arcs, the equator and the poles are projected from one consistent unit sphere. A viewing elevation of 30 degrees separates the arcs. Screen angles need not equal spatial angles, and the projected poles lie inside the circular silhouette. The illustrative point has colatitude 50 degrees and azimuth 38 degrees.

Omitted from the current comparison PDF. Stable identifier: `fourier--fourier-spherical-coordinates`.

Rebuild from the repository root:

```sh
build/figure-runtime/bin/python scripts/regenerate_figures.py --id fourier--fourier-spherical-coordinates
```

Matching asset directory: `figures/chapter-fourier-and-convolution/fourier-spherical-coordinates`. `context.tex` records the mathematical context at reconstruction time. `original/` preserves historical assets and `original.pdf` assembles the previous figure. `proposed.pdf` is used by the reading editions only when the manifest state is `accepted`.

The TikZ sources and `proposed.tex` specify the editable drawing and its assembly.

# Figure 2.15

Fourier and Convolution.

## Figure 2.15

Previously accepted TikZ drawing retained in the reading editions.

Exact current book caption (LaTeX):

```tex
The two-dimensional torus $\TT ^2=(\RR /2\pi \ZZ )^2$.\relax
```

Regenerated from the accepted editable TikZ figure; its geometry is retained. Opposite sides of the square carry matching oriented arrows. Identifying each pair produces the two-dimensional torus. The pictured doughnut is a topological visualization, not an isometric embedding of the flat quotient.

Omitted from the current comparison PDF. Stable identifier: `fourier--fourier-torus`.

Rebuild from the repository root:

```sh
build/figure-runtime/bin/python scripts/regenerate_figures.py --id fourier--fourier-torus
```

Matching asset directory: `figures/chapter-fourier-and-convolution/fourier-torus`. `context.tex` records the mathematical context at reconstruction time. `original/` preserves historical assets and `original.pdf` assembles the previous figure. `proposed.pdf` is used by the reading editions only when the manifest state is `accepted`.

The TikZ sources and `proposed.tex` specify the editable drawing and its assembly.

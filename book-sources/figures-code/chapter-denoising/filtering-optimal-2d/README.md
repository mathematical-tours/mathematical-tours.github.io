# Figure 7.10

Denoising.

## Figure 7.10

Merged into the preceding figure; no separate comparison page.

Last published caption (LaTeX):

```tex
Noisy image (left) and filtered image (right), using the SNR-optimal width.\relax
```

Omitted from the current comparison PDF. Stable identifier: `denoising--filtering-optimal-2d`.

Matching asset directory: `figures/chapter-denoising/filtering-optimal-2d`. `context.tex` records the mathematical context at reconstruction time. `original/` preserves historical assets and `original.pdf` assembles the previous figure. `proposed.pdf` is used by the reading editions only when the manifest state is `accepted`.

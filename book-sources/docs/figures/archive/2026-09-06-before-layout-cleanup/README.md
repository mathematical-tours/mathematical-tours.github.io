# Verification before the directory cleanup

This record describes the accepted figure publication immediately before the source/tool/review directories were reorganized. Its paths refer to the former layout. Current publication verification is in `../../verification.json`; the cleanup map is in `../../../repository-layout.md`.
